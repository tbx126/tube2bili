# 必须修改。仅从内网访问，不要在路由器上做公网端口转发。
DASHBOARD_PASSWORD=replace-with-your-long-password
PORT=8080
BIND_ADDRESS=0.0.0.0
# 可替换为 NAS 的绝对目录，如 /volume1/docker/tube2bili/data
MEDIA_ROOT=./data
# 使用 HTTPS 反向代理时设为 1
COOKIE_SECURE=0
